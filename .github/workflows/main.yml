name: Continuous deployment

on: [pull_request, issues]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
    - uses: github.com/RMorjane/simplon-devcloud-elearning.git@main
    - name: docker compose build
      run: docker-compose up --build

    - name: test
      run: python3 -v
